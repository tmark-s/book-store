<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Book-Store', root_path, class: "navbar-brand", id: "logo" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <% if user_signed_in? && current_user.admin? %>
        <ul class="nav navbar-nav">
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Users<span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to 'Create new user', new_user_registration_path %></li>
              <li><%= link_to 'View all users', users_path %></li>
            </ul>
          </li>
        </ul>
      <% end %>

      <ul class="nav navbar-nav">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Categories<span class="caret"></span></a>
          <ul class="dropdown-menu">
            <% if user_signed_in? && current_user.admin? %>
              <li><%= link_to 'Create new category', new_category_path %></li>
            <% end %>
            <li><%= link_to 'View all categories', categories_path %></li>
          </ul>
        </li>
      </ul>

      <ul class="nav navbar-nav">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Books<span class="caret"></span></a>
          <ul class="dropdown-menu">
            <% if user_signed_in? && current_user.admin? %>
              <li><%= link_to 'Create new book', new_book_path %></li>
            <% end %>
            <li><%= link_to 'View all books', books_path %></li>
          </ul>
        </li>
      </ul>

      <% if user_signed_in? %>
        <ul class="nav navbar-nav">
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Wishlists<span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to 'View all wishlists', wishlists_path %></li>
            </ul>
          </li>
        </ul>

        <ul class="nav navbar-nav">
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Buy history<span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to 'View all histories', history_path %></li>
            </ul>
          </li>
        </ul>

        <ul class="nav navbar-nav">
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Receipt<span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to 'View all receipts', receipts_path %></li>
            </ul>
          </li>
        </ul>
      <% end %>


      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li><%= link_to "<span class='glyphicon glyphicon-shopping-cart glyphicon-size'>#{current_user.shoppingcarts.count}</span>".html_safe, shoppingcarts_path %></li>
          <li class="dropdown">
            <a herf="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= "#{current_user.firstname}  #{current_user.lastname}" %><span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to 'Edit your profile', edit_user_registration_path %></li>
              <li><%= link_to 'View your profile', user_path(current_user) %></li>
              <li><%= link_to 'Logout', destroy_user_session_path, method: :delete %></li>
            </ul>
        <% else %>
          <li><%= link_to 'Log in ', new_user_session_path %></li>
          <li><%= link_to 'Sign up', new_user_registration_path %></li>
        <% end %>
      </ul>

    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>